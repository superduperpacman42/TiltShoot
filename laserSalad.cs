using UnityEngine;
using System.Collections;

public class laserSalad : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		RaycastHit rt;
		//if (Physics.Raycast (transform.position, transform.forward, out rt))

	}
}
